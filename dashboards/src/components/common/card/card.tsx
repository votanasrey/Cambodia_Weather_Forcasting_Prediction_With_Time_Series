import { Box } from "@chakra-ui/react";
import React from "react";

const Card: React.FC = () => {
  return <Box w={300} h={300} bg="black" shadow="base" borderRadius={8}></Box>;
};

export default Card;
